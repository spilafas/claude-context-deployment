# Milvus Stack Configuration
# ==========================

# Data directories (relative to docker-compose.yml location)
ETCD_DATA_DIR=./data/etcd
MINIO_DATA_DIR=./data/minio
MILVUS_DATA_DIR=./data/milvus

# MinIO credentials (object storage)
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin

# Milvus performance tuning
# Query cache: 4GB (4294967296 bytes)
QUERY_MEMORY_LIMIT=4294967296
# Index cache: 2GB (2147483648 bytes)
INDEX_MEMORY_LIMIT=2147483648

# Claude Context MCP Configuration
# =================================

# Milvus connection
MILVUS_ADDRESS=http://localhost:19530
MILVUS_TOKEN=

# Ollama configuration
OLLAMA_BASE_URL=http://localhost:11434
EMBEDDING_PROVIDER=ollama
EMBEDDING_MODEL=nomic-embed-text

# Alternative embedding models (uncomment to use):
# EMBEDDING_MODEL=mxbai-embed-large
# EMBEDDING_MODEL=all-minilm

# Optional: Enable debug logging
# DEBUG=true
